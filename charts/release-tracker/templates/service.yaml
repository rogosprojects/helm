apiVersion: v1
kind: Service
metadata:
  name: {{ include "release-tracker.fullname" . }}
  labels:
    {{- include "release-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "release-tracker.selectorLabels" . | nindent 4 }}

